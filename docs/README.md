# Docs

This is the Fumadocs site for `cambridge-reader-scraper`.

## Local commands

```bash
pnpm --dir docs dev
pnpm --dir docs lint
pnpm --dir docs typecheck
pnpm --dir docs build
```

The production build is a static export that gets published to GitHub Pages from this repo.
